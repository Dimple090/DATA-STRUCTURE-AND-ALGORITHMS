//there are three types of Control structures 
//1:sequence structure(
// Default control structure.
// Statements are executed one after another in the order they appear.)

//2:Selection (Decision-making,conditional ststement) Control Structure(
//Used to make choices based on conditions.
//Types:if,if-else,if-else if-else ladder,switch,Ternary operator (?:))

//3:Iteration (Looping) Control Structure
//Used to repeat a block of code multiple times.
//Types:for loop,while loop,do-while loop

//Jump Control Structure
//Used to alter the flow of execution unconditionally.
//Types:break – exits loops or switch,continue – skips current iteration of loop,goto – jumps to a labeled statement (not recommended),return – exits a function
